/* export function name(a, b) {
    //ne pas oublier le return
  return a + b;
}
 */

function name(a, b) {
  //ne pas oublier le return
return a + b;
}
//utiliser le module jest pour les test
module.exports = name;

